# Image policy

This learning site intentionally uses CSS shapes and typography instead of remote images.
That keeps the static site fast, avoids third-party image requests, and prevents accidental tracking.
