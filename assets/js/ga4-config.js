/**
 * GA4 learning site configuration.
 *
 * Replace the placeholder with your Web data stream's Measurement ID.
 * A GA4 Measurement ID is public in browser code; it is not an API secret.
 */
window.GA4_CONFIG = Object.freeze({
  measurementId: "G-PE33N5234J",
  debug: false,
});
